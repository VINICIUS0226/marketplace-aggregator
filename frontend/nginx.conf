server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # React Router precisa receber index.html também em acessos diretos às rotas.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
