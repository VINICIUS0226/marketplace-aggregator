import {
  Container,
  Typography
} from "@mui/material";

export function Products() {
  return (
    <Container>
      <Typography variant="h4">
        Marketplace Aggregator
      </Typography>
    </Container>
  );
}